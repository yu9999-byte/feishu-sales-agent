# 决策记录

本文件记录已经确认的产品和技术方向。新决定覆盖旧假设时，应同时更新相关架构文档。

## 已确认决策

| 编号 | 决策 | 原因 | 状态 |
| --- | --- | --- | --- |
| D-001 | 产品目标是面向多个企业的 SaaS | 未来需要按企业安装、配置和隔离数据 | 已确认 |
| D-002 | Agent 能力由项目代码实现 | 妙搭只提供托管能力，不能替代 Agent 编排和业务逻辑 | 已确认 |
| D-003 | 目标架构不依赖妙搭运行时 | 便于独立部署、接收开放平台回调和演进多租户能力 | 已确认 |
| D-004 | P0 不做独立 Web 页面 | Demo 聚焦飞书内真实闭环，避免先做低价值页面 | 已确认 |
| D-005 | P0 使用机器人和消息卡片交互 | 机器人负责输入，卡片负责有边界的业务确认 | 已确认 |
| D-006 | P0 以多维表格保存模拟客户、商机和跟进 | 当前不接 CRM，Base 可直接查看和校验数据 | 已确认 |
| D-007 | P0 确认后创建真实飞书任务 | 证明 Agent 不只会回答，还能推动可执行动作 | 已确认 |
| D-008 | 所有内部状态带租户边界 | 即使 P0 只在一个真实企业演示，也必须避免形成单租户死路 | 已确认 |
| D-009 | P0 只实现输入到行动闭环 | 不做语音、会议、经营分析、Playbook、计费和市场发布 | 已确认 |
| D-010 | Goal v3 的机器人输入在原飞书聊天完成草案编辑、再质检和确认；Web 非必经页 | 输入和反馈在同一触点 | 文本链路已实现并通过真实飞书 UI 验收 |
| D-011 | 草案卡预览本人待办，一次确认授权保存跟进并创建所示任务 | 同时满足确认边界与保存后自动创建 | 已实现并通过真实飞书 UI、Base 和任务对账 |
| D-012 | 保存前内容质检与保存后项目推进质检分开；三种跟进卡按事件发，不固定三张 | 质量改进、执行结果和后续风险有不同生命周期 | 正常两卡已真实验收；条件风险第三卡仍待风险样本 UI 验收 |
| D-013 | 跨角色通知按确认事实、证据、租户策略及资源权限执行；任务状态先建议后确认 | 防止越权广播和未经授权改变任务 | 已确认，待实现 |
| D-014 | 卡片状态更新使用持久化消息 ID 调用消息 patch；回调同步响应只负责快速反馈 | 回调响应可能覆盖异步更新，导致数据库已成功但原卡仍可操作 | 已实现并通过真实飞书 UI 验收 |
| D-015 | 卡片日期控件先按租户业务时区格式化，不把 UTC 时刻直接当本地时间 | 避免日期控件把 14:00 显示成前一日/错误时刻 | 已实现并通过 2026-09-22 14:00 UI 对账 |
| D-016 | 机器人先做对话意图路由；只有明确记录或当前跟进补充态才进入跟进抽取 | 普通聊天/问答/查询不应误生成草案或外部副作用 | 已实现；问候、问答、生成、查询、歧义和明确跟进已通过真实飞书首轮 UI |
| D-017 | 保存前卡默认显示可用性、阻断项和建议，不突出 0–100 分/A–D；项目诊断、销售辅导分开 | 避免把记录完整度、项目健康与销售表现混作考试 | 无评分主视觉、中文建议及无内部字段名均已通过真实飞书 UI |
| D-018 | 取消动作必须在回调返回后延迟持久 patch 原消息，重复取消可幂等自愈；本次沟通上下文由结构化契约提取并按业务时区输出，不向 UI 泄漏内部字段名 | 同步 patch 可能被回调后的卡片状态覆盖；缺少上下文会导致错误补充建议和时间错位 | 已通过 121 项回归及真实飞书 UI 验证；取消终态持久且未触发 Base/Task |
| D-019 | 普通问答先给同聊天文本等待态，完成后编辑为最终答案；不展示隐藏推理 | 模型耗时期间应有接收反馈，同时保持自然聊天而非为每次问答发多张卡 | TDD 与 126 项回归通过；真实飞书等待态待复验 |
| D-020 | “记录还是分析”建立受租户/用户/聊天/TTL 约束的短期澄清会话；确认记录只复用原文，过期待办禁用但不阻止保存跟进 | 多轮确认必须承接原消息，同时把历史事实保存与未来任务授权分离 | 已实现并通过 133 项回归与 8 项 Postgres 集成；真实飞书复验待确认发送 |
| D-021 | “确认保存”始终是一次性执行门；编辑后也不回退为“检查修改”；成功卡提供 Base 跟进记录链接 | 按钮意图必须优先于“回调值是否变化”；用户需要可感知、可跳转的成功凭证 | 一次执行与链接已真实验证；结果呈现方式由 D-022 覆盖 |
| D-022 | 正常确认只把原草案卡就地更新为唯一“跟进登记成功”卡；原卡更新失败时才补发结果卡 | “原卡终态 + 新结果卡”会把同一结果重复展示；一个业务动作只应有一个主结果凭证 | 定向回归通过；真实飞书单卡复测待完成 |
| D-023 | 歧义澄清只描述用户可选动作，不向销售表述“不会直接写入”等内部控制策略 | 安全边界应由系统执行，界面文案应自然承接用户语境 | 定向回归通过；真实飞书文案待复测 |
| D-024 | 临时公网隧道必须视为运行时依赖；回调地址变更后必须用后台回读和 challenge 双重确认，并保留入口到工作流日志 | Tunnelmole 退出会让飞书事件日志为空、消息完全无回复；仅看本地 3100 或页面能打开不足以证明投递链路 | 已切换 WebSocket 长连接；两条真实私聊已进入并完成工作流，卡片 UI 对账仍待完成 |
| D-025 | [历史口径] 意图识别改为 AI 优先、代码负责状态和副作用；引入租户/用户/聊天隔离的短期对话记忆；其中“P0 暂不引入外部记忆框架”已被 D-028 覆盖 | 关键词门会把“帮我写跟进”误判为换话题并丢失上下文；AI 优先和隔离原则继续有效 | AI 优先、隔离、确认前零副作用继续有效；外部记忆部分已被 D-028 覆盖 |
| D-026 | 跟进与待办采用一张整表一次提交；确认后同一卡立即进入“执行中，请等待”，后台结束后就地更新为成功/失败。成功和失败都可回到预填整表；成功修订更新原跟进及原任务，不创建重复记录 | 当前回调先 patch 再执行，执行中态容易不可见；双提交增加认知负担；只有查看链接不满足结果可编辑 | 已确认；SDD、TDD/Green/Runtime 已完成，真实飞书 UI 待验收 |
| D-027 | Agent 运行时采用 LangChain.js + LangGraph.js；LangGraph StateGraph 成为唯一生产对话编排，LangChain 负责模型适配和结构化输出 | 当前自研对话中枢难以稳定承接上下文、暂停恢复和扩展意图；采用成熟开源抽象降低长期维护成本 | 已确认；依赖、代码迁移和运行验证已完成，真实飞书 UI 复测待完成 |
| D-028 | 记忆采用分层替换：LangGraph Postgres Checkpointer 负责短期 thread/工作流状态，Mem0 OSS 负责受策略控制的长期语义记忆；Base/Task/控制库继续作为业务事实源 | Checkpointer 是状态持久化而非长期语义记忆；Mem0 提供开源长期记忆能力；替换当前自研同职责路径，不保留双写双读 | 已确认；选型和边界已写入 SDD，Mem0 评测与迁移待执行 |
| D-029 | 正式确认保留 Postgres Checkpointer 作为唯一工作流状态层，长期项目优先采用 Mem0 OSS 作为唯一长期语义记忆框架；P0 暂不启用 Mem0 生产写入 | 两者职责不同，强行二选一会损失 LangGraph 的暂停/恢复能力；当前先保证 P0 可运行，再以评测结果决定长期记忆上线 | 已确认；文档、端口和默认关闭适配器已完成，向量后端和评测仍待执行 |
| D-030 | Mem0 只能通过长期记忆端口接入，默认关闭；只允许已确认的销售/用户偏好，强制 tenant + actor ACL 和来源/策略元数据 | 让长期记忆可替换、可删除、可审计，避免模型把未经确认的客户事实写成记忆或跨租户召回 | 已实现适配器与基础 TDD；业务确认后的异步写入和真实向量后端评测待执行 |
| D-031 | LLM 是自然语言意图主分类器；关键词只允许在模型已路由后提取内联正文，不再作为进入跟进流程的前置门 | 解决“帮我写一下/那就记录下来”等自然表达被关键词拦截；代码仍保留确认门和副作用边界 | 已实现并补回归；机器人角色权限校验仍按用户要求暂缓 |
| D-032 | `executing` 以配置化超时为上限，回收为 `failed/partialFailure` 并保留重试；执行结果使用状态 CAS | 进程崩溃或外部调用卡住不能让动作永久不可操作，旧 Worker 不能覆盖新终态 | 已实现，默认 5 分钟，可由 `AGENT_EXECUTION_TIMEOUT_MS` 覆盖 |
| D-033 | Web 确认后的待办候选按逻辑动作跨版本校验，并优先复用已创建动作的候选 | 确认会产生版本 N+1，页面 reload 后候选 ID 变化不应让幂等重试必然失败 | 已修复；失败动作复用原候选，确认后未建动作的崩溃窗口会映射逻辑任务 ID |
| D-034 | Mem0 OSS 从适配器升级为受控业务能力：`memory_save` 明确批准写入，分类前按 tenant + actor 召回；默认关闭外部后端 | 让长期记忆真正进入对话链路，同时不把客户事实写入语义记忆，也不阻塞主流程 | 代码已接入，真实 Qdrant/embedding 评测仍是上线门禁 |
| D-035 | Web OAuth state 绑定短时 HttpOnly 浏览器 cookie，回调校验后立即清理 | 防止登录 state 被跨浏览器/跨会话复用；不改变租户成员权限模型 | 已修复并覆盖缺 Cookie、不一致和重放；机器人入口角色权限校验仍按要求暂缓 |
| D-036 | 补充态消息也必须先经 LangChain/LangGraph 主分类；代码正则不再决定是否换话题 | 避免未命中固定前缀的普通问题被误写进跟进上下文 | 已实现；普通问答退出补充态，相关补充继续生成草案 |
| D-037 | LangGraph thread 默认 24 小时 TTL；超时后调用 Checkpointer `deleteThread` 再开始新上下文 | 防止过期销售对话长期污染意图判断，并给 checkpoint 数据增长设置边界 | 已实现；TTL 内保留、超过 TTL 删除均有回归测试 |
| D-038 | 正式数据库结构只通过 `migrate:agent` 执行 `003/005/006/007/008`，以文件名和 SHA-256 建迁移账本；本地租户种子排除在外 | 让空库和已迁移库具备同一可重复入口，并防止已执行迁移被静默修改 | 已实现并在本地 Postgres 连续执行两次验证 apply/skip |
| D-039 | S4 只接受显式生命周期字段及状态值映射；禁止从“当前进展”自由文本猜测 active/won/lost/closed | 错判已结束商机会造成错误主动提醒，风险高于暂时不提醒 | 扫描契约和 Base 适配器已 fail closed；真实表待新增字段和映射 |

以上 D-010..013 的问题、取舍、被覆盖旧口径与验收见
[跟进卡片决策问答](decisions/2026-09-20-followup-chat-cards.md)。D-004、D-009 仅描述
历史 P0 范围，不限制后续 Goal v3 的可选 Web 页面、多模态、报告等能力。
D-016..020 的覆盖口径与取舍见
[对话与质检决策问答](decisions/2026-09-20-conversation-and-quality.md)。
D-021..023 的按钮意图、唯一结果卡、记录链接和澄清文案详细问答见
[跟进卡片决策问答](decisions/2026-09-20-followup-chat-cards.md)。
D-025 的意图识别和记忆边界见 [意图识别 SDD](specs/07-intent-recognition-sdd.md) 与
[对话记忆 SDD](specs/08-memory-sdd.md)。
D-026 的整表单次提交、执行态和结果修订问答见
[执行反馈决策问答](decisions/2026-09-23-action-execution-feedback.md)。
D-027..034 的框架替换、Checkpointer/Mem0 分层、意图主分类、执行恢复和版本修复见
[LangChain/LangGraph 与记忆决策问答](decisions/2026-09-23-langchain-langgraph-memory.md)
和 [LangGraph Agent Runtime SDD](specs/10-langgraph-agent-runtime-sdd.md)。D-025 中“P0
暂不引入外部记忆框架”已被 D-028 覆盖；其 AI 优先意图原则和租户/用户/聊天隔离原则继续有效。

## 妙搭资产处置

已有妙搭应用 `销策 Agent` 和 React/NestJS 脚手架暂时保留，防止误删已有资产。目标架构
不再使用妙搭页面托管、妙搭数据库或妙搭 AI PluginInstance。后续只有在独立后端跑通并经
用户确认后，才决定删除、归档或复用其中的通用代码。

## 多企业 SaaS 与 P0 Demo 的关系

- 产品目标：同一套服务可处理多个飞书企业安装实例。
- P0 真实验收：先在一个测试企业的自建应用中跑通端到端流程。
- P0 自动验收：使用两个模拟 `tenant_id` 验证配置、会话、幂等键和数据访问相互隔离。
- P0 不包含：飞书应用市场上架、商业授权、计费、企业自助安装和管理员配置页面。

当前自建应用只能服务其所属企业，不能单独证明跨企业分发能力。生产化时需要迁移到可被
多个企业安装的飞书商店/ISV 应用形态，或提供每企业独立应用配置。

## 数据源决策

P0 每个企业绑定自己的多维表格，后端通过以下映射访问数据：

```text
tenant_id -> data_source_config -> base_token -> customer/opportunity/followup tables
```

真实演示只创建一个 Demo Base。未来 CRM、数据库和其他 Base 通过统一数据源适配器接入，
不把某个具体 Base 的字段名写死在 Agent 推理逻辑中。

## 安全记录

用户提供的飞书应用 Secret 曾出现在对话中，应视为已暴露。接入前必须在飞书开放平台重置，
新 Secret 只允许通过本地环境变量或密钥管理服务配置，不得写入 Markdown、源代码或 Git。

## 后续确认门

1. 先人工确认本轮产品、业务、技术和交互文档。
2. 文档确认后再开始代码和飞书资源写入。
3. 创建多维表格前展示表结构；创建行为以已确认 Goal 为授权范围。
4. 部署、开放可见范围和应用市场操作仍需单独确认。
