# Mem0 长期语义记忆 TDD

状态：`Green / 业务接入完成；真实后端评测待执行 / 2026-09-23`

## 1. 测试边界

当前测试使用真实端口和默认关闭配置，不连接 Qdrant、embedding 或外部模型。真实 Mem0
集成测试必须单独放在 integration 套件，并通过显式环境开关运行，不能让普通单元测试访问
外部服务。

## 2. Red/Green 用例

| 用例 | 预期 | 当前状态 |
| --- | --- | --- |
| 默认关闭时构造适配器 | 不初始化 Mem0，写入返回 `disabled` | 已完成 |
| 默认关闭时检索/删除/历史 | 返回空结果，不发外部请求 | 已完成 |
| 缺少 tenant 或 actor | 在任何操作前拒绝 | 已完成 |
| 未显式批准的记忆 | 拒绝写入 | 已完成 |
| 跨 tenant 搜索 | 过滤并返回空，不暴露存在性 | 已完成 fake client 测试 |
| 跨 actor 删除/历史 | 返回 `false`/空，不暴露存在性 | 已完成 fake client 测试 |
| customerRef 缩小范围 | 只返回同一客户引用 | 已完成适配器过滤；专门 customerRef 样本待补 |
| 明确批准偏好写入 Mem0 | 只写当前 actor/tenant，返回 memory id | 已接入 `memory_save` 工作流 |
| Mem0 检索失败 | 当前对话继续，不阻塞模型 | 已接入降级路径 |
| 业务动作成功但 Mem0 写失败 | 业务结果保持成功，记录非敏感告警 | 已接入端口级降级；业务动作不依赖记忆 |
| 过期记忆不召回 | 默认过滤过期内容 | 待真实后端评测 |
| 中文召回 | 通过项目设定的准确率/延迟门槛 | 待真实后端评测 |

## 3. 接入前验收

- 全量 21 个测试文件、150 项通过（Vitest 使用固定的 `threads` pool）；
- `npm run type:check:server` 和 `npm run type:check:test` 通过；
- `npm run eslint` 通过；
- 使用 Qdrant 等持久后端完成两租户隔离和删除测试；
- 业务确认门、审计和降级路径有真实飞书端到端证据。

## 4. 后续实现顺序

1. 以 fake Mem0 client 补齐 ACL、删除、history 和失败降级测试；
2. 决定并部署持久向量后端，完成 embedding 配置；
3. 在真实向量后端完成中文召回、过期、容量和恢复演练；
4. 通过评测后再将 `MEM0_ENABLED` 在目标环境开启；默认关闭策略不变。
