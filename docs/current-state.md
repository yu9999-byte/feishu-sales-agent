# 当前工程说明

## 2026-09-26 跟进沟通时间字段落地

- 真实 Demo Base 的“跟进”表已新增并回读验证字段“本次沟通发生时间”，类型为
  `datetime`，显示格式为 `yyyy-MM-dd HH:mm`，字段 ID 为 `fld28D7L08`。
- 租户映射、Base 读取和跟进创建/更新链路已接入该字段。只有销售在确认的跟进草案中提供
  明确沟通时间时才写入；没有明确时间时保持为空，不使用消息接收时间或系统当前时间补造。
- 本次没有修改任何历史跟进记录。历史记录只有在原始证据明确给出发生时间时才允许后续单独
  回填，无法核实的记录保持未知并跳过 S4 判定。
- S4 仍未接入扫描、去重、提醒投递或触发器；Task v2 的 `task:task:read` 权限仍未开通，
  因此前置条件尚未满足。

## 2026-09-26 S4 商机级有效跟进汇总前置能力（未接入）

- 新增纯内存 `StaleOpportunityContextService`：接收跟进记录的商机关联、明确沟通时间和
  来源版本，跳过缺少任一可信字段的记录，按商机保留最新有效沟通，并输出可供后续判定层
  使用的来源记录 ID、规范化时间和版本。
- 该服务不读取真实 Base、不写入 Base/Task/控制库、不发送提醒，也不判断商机是否已经停滞；
  它只解决“逐商机最近有效沟通时间如何从跟进记录得到”的可测试边界。新增 4 项单测，
  与 S4 判定层合计全量 Agent `219/219` 通过，服务端/测试类型检查和 ESLint 通过。
- 新增 `FeishuBaseGateway.readStaleOpportunityFollowupPage` 只读分页接口：按当前销售负责人
  过滤跟进表，单页最多 500 条，读取商机关联、明确沟通时间和修改版本，并返回下一页 token。
  缺少负责人/必要字段映射或 API 分页不完整时显式返回 warning；不写 Base、Task 或控制库。
- 该分页接口及跨页汇总测试已通过；Task `task:task:read` 权限、定时/事件触发、持久去重和
  本人提醒仍未完成，S4 继续保持 `Partial / disabled / prerequisites open`。

## 2026-09-26 S4 本地判定层（未接入、未启用）

- 新增纯判定层 `StaleOpportunityDecisionService`。只有开关开启、本人负责的有效商机、
  逐商机可信有效跟进时间已超过完整 7 天、本人任务查询完整且无相关未完成任务时，
  才输出带商机及跟进来源的提醒候选；周末或租户本地 09:00-18:00 之外只延后。
  日期无时区、未来时间、缺少来源版本、权限/任务可见范围不完整、负责人不符均跳过。
- 这是未接入运行模块的离线判断能力，不读取真实 Base/Task、不创建触发器或去重记录，
  也不发送提醒。`enabled` 是输入边界，不是已经存在的运行配置。S4 仍为
  `Partial / disabled / prerequisites open`，不能当作主动 Agent 已上线。
- 新增 6 项边界测试，Agent 全量 `211/211`、服务端/客户端/测试类型检查和 Agent/Web
  构建通过；真实 Base 字段和 Task 应用权限仍以本文件下节的只读证据为准。
  最新飞书卡片视觉验收仍未完成。

## 2026-09-26 真实飞书回归问题修复与 S4 前置状态

- 用户本人发送的 P0 跟进测试消息已到达 Agent，生成了待确认草案卡；未点击确认，
  因此这条消息没有写入 Base、创建飞书任务或发送客户消息。原卡错误显示客户上下文不完整、
  无任务候选。数据库草案快照证明 `customer_not_found`，不是任务查询告警导致的整体判断失败。
- 根因：Base 只读搜索返回的文本单元格为 `[{text,type}]`，旧适配器只识别字符串或用户
  `name`，把存在且归属本人的“华南科技”当成未找到；模型偶发把“这是 P0 测试”当成商机，
  或把“报价方案”误作精确商机名。现兼容富文本返回；商机提示精确匹配为空时仍按客户关联、
  本人负责人范围查询，不扩大租户/所有权边界；测试元备注不作为商机名。
- 实际 Task v2 搜索返回 `99991672`：当前销售agent应用身份缺少 `task:task:read`。
  现在明确提示“本人任务读取权限未开通”。任务或商机无法核实时可保留基于本次沟通的建议，
  但不生成可执行/默认勾选的待办候选；权限开通后需重新生成草案，旧卡不会自动补全。
- 原文仅称“下周五前”，没有具体钟点。已禁止将消息发出时刻当作截止时间；草案时间为空，
  请销售选择具体执行时间。此限制不改变具有明确钟点的“下周五 10 点”处理。
- 使用同一原文、同一时间点、真实 Base/Task/模型进行无写入重放：客户“华南科技”、唯一商机
  “华南科技 - 销售机会”、风险判断和发送资料/复盘建议均读出；任务权限告警可见，
  `dueAt=null`、任务预览关闭。Agent `203/203`、Postgres 集成 `8/8`、三套 TypeScript、
  ESLint 和 Agent/Web 构建通过。该重放不是最新卡片视觉 UI 验收，状态仍为 `UI pending`。
- 旧真实测试草案 `ef46ee37-ae8b-4320-a818-ac06bb5caa49` 曾保留错误时间并在有效期内；
  已核对租户、本人和来源消息后仅取消这一条待确认动作，追加取消审计。新 Agent PID
  `61196` 已接管端口 `3100`；首页 HTTP 200、飞书长连接成功，启动 stderr 为空。
- S4“商机超过 7 天未更新”尚未创建或启用触发器。当前商机表仍没有逐商机最近有效跟进
  时间，跟进表虽已新增“本次沟通发生时间”，但历史数据没有自动回填，且 Task 读取权限未
  开通；字段结构已在真实 Base 只读核对。详见
  [S4 触发规格](specs/18-stale-opportunity-trigger-sdd.md)。未达前置条件前不自动发提醒。

## 2026-09-25 Goal v3 P1-PROGRESS 自动化实现

- 销售提交跟进后，Agent 现在基于本次原文、唯一匹配客户/商机、近期跟进和本人任务生成
  版本化推进判断，分别展示“事实依据、Agent 判断、建议下一步、确认后执行”。
- 判断覆盖真实进展变化、预算/决策链/下一步缺口、本次风险、竞品、本人逾期任务、来源冲突、
  多客户匹配和部分来源失败；只有本次进展与匹配商机当前进展确实不同时才标记新进展。
- 无唯一客户、业务上下文不可用或明确暂无下一步时不生成行动建议和新任务候选；旧草案没有
  `progressAssessment` 时仍可读取。
- Web 编辑会重新计算并持久化新快照；确认版本复制同一快照。已修复数据库编辑曾复制旧版
  `progress_assessment` 的问题，并由不同内容的新旧快照集成断言防回归。
- 飞书卡片与 Web 草案已接入四层展示；未发送真实飞书验收消息、未对真实 Web 页面做本轮对账，
  因此状态为 `Automated Green / UI pending`，不能标记 `UI Verified`。
- 自动化证据：Agent `191/191`、Postgres 集成 `8/8`、三套 TypeScript、ESLint、Agent/Web
  构建通过；迁移 `010_followup_progress_assessment.sql` 首次 `apply`、第二次 `skip`。
- 本轮问题与状态见 [问题日志](issue-log.md)。定时扫描“商机超过 7 天没有更新”仍属于 S4，
  未在 Goal v3 内实现。

## 2026-09-25 Goal v2 产品路线冻结

- 产品主线统一为 **销售跟进 Agent：持续执行闭环**：用户输入客户沟通后，Agent 识别客户、
  商机、跟进和任务，读取业务事实，保存跟进计划，持续判断状态和缺口，并在确认后执行下一步。
- “主动任务跟进 Agent”不再作为并列用户产品；它是 P1 内部的触发/履约能力。商机推进、提醒、
  Review 和 Playbook 也按能力切片管理，不能替代主线定义。
- Goal v2 当前第一个实现切片为 `P1-CTX`：生成草稿前只读查询客户、商机、近期跟进和飞书任务，
  统一为标准领域上下文并保留来源引用；无匹配、多匹配、权限不足、跨租户和旧信息冲突必须显式处理。
- 冻结时仅有路线与契约，没有把 `P1-CTX` 宣称为已实现；后续首轮实现状态见下节，仍未达到
  UI Verified。

## 2026-09-25 P1-CTX 首轮实现进展

- 新增 `SalesContextService`、Base 只读查询和飞书本人任务搜索；Base 查询强制使用租户映射的负责人字段过滤当前 open_id，缺失负责人映射时不发查询；任务按 assignee 和未完成状态过滤。
- 文本跟进先做不展示的实体预识别，再读取上下文并生成最终草稿；上下文被单独保存在草案 payload，并在确认卡显示记录 ID。提示词禁止把只在历史上下文出现的内容混入本次跟进正文。
- 客户多匹配会返回候选且不继续读取关联任务；查询异常、任务源部分失败、权限错误有显式降级状态；未修改 Base/Task 写操作和确认执行顺序。
- 首轮 Red 因 `SalesContextService` 缺失而失败；当前定向测试覆盖编排、租户/操作者过滤、客户/商机多匹配、失败降级、只读副作用、草稿注入、来源持久化、卡片来源显示，以及商机与跟进行动字段的版本冲突保留。全量自动化 181 项、Postgres 集成 8 项、三套类型检查、ESLint、Agent/Web 构建通过。真实飞书/Web UI 和历史冲突数据评测仍待验收；Web 草案上下文接入进展见下节。

## 2026-09-25 Web 草案上下文接入进展

- Web 草案生成现在先做不持久化的实体预识别，再使用可信 Web session 的租户、成员和飞书 `open_id` 读取本人范围上下文；最终草案才进入版本存储。
- `followup_draft_versions.context_snapshot` 已通过 `009_followup_context_snapshot.sql` 持久化；编辑、确认和重新读取均保留同一份上下文。Web 响应和草案页面展示客户、商机、近期跟进、本人待办、来源链接、冲突和降级警告。
- 新增 Web 工作流单测、草案控制器边界测试和 Postgres 回读断言；迁移后集成测试 `8/8` 通过。真实 Web 登录、真实飞书查询和多分支 UI 对账仍待验收，不能标记 `UI Verified`。

## 2026-09-25 真实飞书 UI 验收结果

- 真实私聊已验证：消息“帮我写跟进：今天和华南科技张总通过飞书沟通，确认下周二上午
  10 点线上演示，下一步发送报价，负责人李胜彬”被直接识别为 `followup_capture`，没有再
  询问“记录还是分析”。
- 控制库时间线确认先创建 `pendingConfirmation`，随后才收到 `confirm_followup_v2` 回调；
  确认后按客户、商机、跟进、待办顺序执行，最终只产生一条跟进记录和一条本人待办。
- 最终卡片显示“跟进登记成功”，并提供跟进记录与飞书任务链接；解析的下一步时间为
  `2026-09-29 10:00`（`Asia/Shanghai`）。原草案卡就地更新为成功卡，因此聊天中最终只保留
  终态卡。
- 真实 UI 验收证据：飞书消息位置 88 的用户原文、位置 89 的识别等待文本、位置 90 的
  成功卡；对应审计为 `message.pending_confirmation` → `followup.version.confirmed.v1`
  → `action.succeeded` → `card.source_finalized`。未发现重复写入。

## 2026-09-25 截图回归与运行态切换

- 截图中的完整命令“帮我写跟进：今天和华南科技张总通过飞书沟通，确认下周二上午
  10 点线上演示，下一步发送报价，负责人李胜彬”已加入工作流回归测试；测试确认不调用
  对话模型，直接进入 `followup_capture` 草案链，不再先问“记录还是分析”。
- 发现并修复 3 个测试因写死 2026-09-24 会话时间而在 2026-09-25 被误判过期的问题；
  仅改为使用当前测试时间，生产 TTL 规则未放宽。
- 最新 Agent 已重新构建并运行于 PID `10408`、端口 `3100`，启动 stderr 为空，日志确认
  `Feishu long connection connected`。此前截图对应旧运行版本；当前数据库审计已记录同类
  命令为 `message.intent_routed.v1 / followup_capture`，随后生成待确认卡。
- 本轮自动化证据为 22 个测试文件、169/169 通过，服务端/客户端/测试 TypeScript、
  ESLint 和 Agent 构建通过。真实飞书 UI 的新消息复测仍需单独取得用户授权后进行，不能
  用历史截图或后端日志替代。

## 2026-09-24 意图路由与飞书接入修复

本轮定位到两个相互独立的问题：明确的“帮我写/记录跟进”指令被模型当成歧义问题，
以及飞书后台仍指向已退出的 Tunnelmole 地址，导致消息根本没有进入 Agent。现已完成：

- 明确跟进命令由确定性路由优先处理；带正文直接生成跟进草案，不带正文打开录入表单，疑问句和真正含糊的表达才交给意图模型。
- 日期解析统一使用 `Asia/Shanghai`，确认前仍不写入 Base 或创建飞书任务。
- 本地接收模式切换为飞书 WebSocket 长连接，后台事件订阅和卡片回调均已切换为长连接，应用版本 `1.0.2` 已发布。
- Agent PID `41356` 日志记录 `Feishu long connection connected`，并已收到两条真实私聊消息，均出现 `message mapped`、`workflow started`、`workflow completed`。这证明事件已送达并完成工作流；具体卡片视觉内容和确认动作仍需在飞书聊天中继续做 UI 对账。

本轮自动化证据为 22 个测试文件、168/168 通过，服务端/客户端 TypeScript、ESLint 和发布门禁均通过。

## 2026-09-24 全量复查整改

已按 [项目复查整改 SDD](specs/13-project-hardening-sdd.md) 完成自动化 Green：OAuth 回调强制
校验浏览器 state Cookie；Web 确认先返回 `executing` 并后台执行，页面自动轮询；失败动作复用
原任务候选，确认 N+1 后未创建动作的崩溃窗口按逻辑任务 ID 映射；跟进补充态全部先经过
LangChain/LangGraph；checkpoint 默认 24 小时过期并调用 `deleteThread` 清理。

新增 `npm run migrate:agent`，只按顺序执行 `003/005/006/007/008`，以文件名和 SHA-256 记录
迁移账本，明确排除本地租户种子。本机 Postgres 首次登记后再次运行全部 `skip`。当前质量门为
22 个单测文件 `168/168`、Postgres 集成 `8/8`、三套 TypeScript、ESLint、Agent/Web 构建通过。
最新构建的运行态和真实飞书 UI 仍需复验。

飞书机器人入口的成员/角色权限校验继续按用户决定只记录，不在本轮修复。Mem0 真实后端仍未
启用，不能将当前状态描述为长期语义记忆已上线。

## 2026-09-23 LangChain/LangGraph 迁移目标与进展

当前增量目标已切换为：按 D-027、D-028 将自研对话中枢和自研短期记忆替换为
LangChain.js + LangGraph.js 运行时；使用 LangGraph Postgres Checkpointer 承接短期 thread
状态和暂停/恢复，使用 Mem0 OSS 承接受策略控制的长期语义记忆。Base、飞书 Task、PendingAction
和审计仍是业务事实、执行授权和可追溯性的来源。

### 已完成

- [x] 用户确认采用 LangChain.js 与 LangGraph.js。
- [x] 明确 Postgres Checkpointer 是工作流状态持久化，不是完整长期语义记忆框架。
- [x] 选定 Mem0 OSS 作为长期语义记忆目标框架，并记录 Apache-2.0、自托管和成本边界。
- [x] 新增 [框架与记忆决策问答](decisions/2026-09-23-langchain-langgraph-memory.md)。
- [x] 新增 [LangGraph Agent Runtime SDD](specs/10-langgraph-agent-runtime-sdd.md)。
- [x] 更新 D-025 历史口径，明确其“P0 不引入外部记忆框架”已被 D-028 覆盖。
- [x] 锁定 LangChain/LangGraph/Checkpointer/Mem0 依赖版本，并完成 LangGraph Postgres schema 初始化。
- [x] 先用 Red 复现 thread 边界缺失会落入 `unknown-*`，再改为显式拒绝；Graph 测试覆盖
  checkpoint 持久化、多轮恢复、跨租户隔离、边界拒绝和意图提示词正反例。
- [x] LangGraph StateGraph 已成为唯一生产对话编排；旧 HTTP 意图助手、旧短期记忆生产读写和
  工作流双写已移除。
- [x] 真实 GLM 已验证问候为 `general_chat`，两轮“客户事实 -> 帮我写跟进”为
  `ambiguous -> followup_capture`；非法枚举会被 structured output Schema 拒绝。

### 当前 TODO

- [ ] P1：飞书机器人入口的成员/角色权限校验（本轮按用户要求只记录，不修复）。
- [x] Web OAuth state 缺 Cookie、不一致或重放均在换取身份前拒绝。
- [x] 接入 Mem0 ACL 召回和 `memory_save` 明确批准写入；默认外部后端仍关闭。
- [ ] 对 Mem0 做中文召回、误记忆、删除、ACL 和容量评测；评测未通过前保持关闭写入。
- [x] 执行动作超时恢复、Web 候选版本重试和补充态 LLM 主路由均完成自动化回归。
- [ ] 在真实飞书 UI 复验执行超时恢复、候选版本重试和自然语言路由。
- [ ] 在真实飞书同一聊天完成普通问答、客户事实澄清和“帮我写跟进”预填表单 UI 验收。
- [ ] 完成确认卡执行中、唯一终态和结果修订的真实 UI 对账。

### 当前阶段

`Automated Green / latest runtime and UI retest pending`。当前 Agent 单元测试 `168/168`、
Postgres 集成 `8/8`，三套类型检查、ESLint、Agent/Web 构建均通过。Mem0 包已安装但生产写入保持关闭：当前 Postgres 没有
`vector` 扩展，且 embedding、中文召回、删除与 ACL 评测尚未完成，因此不把内存向量库冒充长期
持久化能力。

### 本次记忆分层确认（D-029）

用户已确认：保留 Postgres Checkpointer 作为工作流状态层，长期项目优先采用 Mem0 OSS；业务层
不再评估多个长期记忆框架并行接入。当前不需要用户提供新的凭证或配置，下一阶段仅在完成向量
后端、embedding、ACL/删除和中文召回评测后，提交 Mem0 生产写入方案。

## 2026-09-23 当前执行目标与 TODO

当前增量目标：按 D-026 修复“确认后卡片不动、完成后突然成功”，并实现整表一次提交、
同卡执行中/成功/失败反馈，以及成功或失败后的预填修订。

- [x] 完成 `docs/specs/09-action-execution-feedback-sdd.md`
- [x] 同步产品需求、信息架构、决策问答和需求追踪
- [x] TDD Red：执行时序、单按钮、终态编辑、原记录更新
- [x] Green：状态机、卡片、控制库、Base/Task 更新端口
- [x] 全量自动化、类型、Lint、构建与运行日志验证
- [ ] 真实飞书 UI：执行中可见、同卡终态、编辑后原记录更新

当前阶段：`Green verified / runtime UI pending`。完成证据以
[执行反馈 SDD](specs/09-action-execution-feedback-sdd.md) 第 7、8 节为准。

### 本轮继续执行进展（2026-09-23）

- [x] LangGraph Agent PID `46588` 运行于 `0.0.0.0:3100`，启动 stderr 为空。
- [x] 已在飞书开发者后台保存并回读当前临时公网地址：
  `https://xs5byr-ip-183-23-144-230.tunnelmole.net/webhooks/feishu/events`；
  `https://xs5byr-ip-183-23-144-230.tunnelmole.net/webhooks/feishu/cards`。
- [x] 新地址首页返回 HTTP `200`；Agent 日志已收到飞书对 `/events` 与 `/cards` 的
  `POST` challenge/配置探测，Tunnelmole 返回 `201 Created`，说明两个入口均已可达。
- [x] Edge 登录态已打开“销售agent”真实私聊，并回读到历史问答、澄清、跟进和成功卡。
- [ ] 新 LangGraph 构建尚未发送真实验收消息；普通问答、干净双轮承接、预填表单、执行中卡、
  唯一终态和结果修订仍需取得本轮 UI 证据。

下一动作是经用户确认后在真实私聊发送验收消息，并在飞书聊天、Agent 日志、LangGraph
checkpoint、Base 和任务列表对账。旧隧道地址（包括本文后续历史轮次中的地址）仅作历史证据，
不再作为当前回调配置依据。

## 2026-09-23 执行反馈与结果修订实现

- 共享契约新增 `edit` 动作、`operationKind`、`revisionOfActionId`、`executionTarget` 和
  `taskAction`；校验层同步拒绝未知动作或非法外部标识。
- 确认回调现在只完成最终校验、CAS 获取执行权并立即返回“执行中，请等待”卡；后台任务通过
  `setImmediate` 先持久 patch 同一卡，再按客户、商机、跟进、任务顺序执行，最后 patch 唯一
  成功/失败卡。卡片 patch 失败时继续业务执行并保留审计与结果卡兜底。
- 待确认卡只保留一个整表提交按钮；成功/失败卡均提供编辑入口。编辑创建不可变修订动作并原子
  转移卡片消息归属；成功修订调用 Base `PUT` 更新原跟进，已有 Task GUID 调用 Task v2 patch，
  不创建第二条跟进或待办。取消已有任务时保留原任务并在结果中标明未更新。
- TDD 证据：21 个测试文件、150 项测试通过；服务端/测试类型检查、ESLint、Agent/Web 构建和
  `git diff --check` 通过。新增真实 UI 验收仍待在当前公网隧道下执行，不能用自动化结果替代。

## 2026-09-23 回调隧道恢复与普通消息不回复修复

- 根因确认：Agent 进程仍在监听，但飞书事件与卡片回调指向已退出的旧 Tunnelmole 地址，公网请求返回
  `404`，因此普通消息未到达 Agent。
- 已重启最新构建并确认 PID `81748` 监听 `0.0.0.0:3100`，启动 stderr 为空；当前临时公网入口为
  `https://ave5su-ip-121-35-40-33.tunnelmole.net`。
- 已在飞书开发者后台保存并回读确认：
  - 事件：`https://ave5su-ip-121-35-40-33.tunnelmole.net/webhooks/feishu/events`
  - 卡片：`https://ave5su-ip-121-35-40-33.tunnelmole.net/webhooks/feishu/cards`
- 证据：公网首页返回 `200`；两个回调接口对 `url_verification` challenge 均返回原 challenge；
  Agent 日志已记录新的事件和卡片 HTTP 请求，Tunnelmole 日志同样记录 `POST` 到达。
- 该地址是临时隧道，进程退出后会失效；若再次出现“完全不回复”，第一检查项是飞书后台 URL 是否仍与当前运行隧道一致。
- 待完成的人工验收：在飞书“销售agent”私聊发送一条新的普通消息，确认事件进入、等待态/最终回复可见。旧消息不会因修复回调地址而自动重投。

## 2026-09-22 AI 优先意图与短期记忆优化

- 已按 D-025、[意图识别 SDD](specs/07-intent-recognition-sdd.md) 和
  [对话记忆 SDD](specs/08-memory-sdd.md) 完成第一轮实现：自然语言意图由 LLM 结合当前消息、
  同租户/用户/聊天的最近上下文判断；代码只负责 Schema 校验、状态机、权限、确认门、幂等和
  工具副作用。
- 已新增 `followup_analyze` 意图。裸客户事实不会直接写入；有澄清会话时，“帮我写跟进”会
  复用原文生成预填跟进卡，“只分析一下”只返回分析，不创建草案、Base 记录或任务。
- 已新增 Postgres `conversation_turns` 短期记忆及 `migrations/008_conversation_memory.sql`：
  租户、销售、聊天三重隔离，用户/助手角色，24 小时 TTL，消息角色幂等，最多向模型提供最近
  12 条。这是 2026-09-22 旧实现证据；D-028 已决定迁移到 LangGraph Postgres Checkpointer，
  并评测 Mem0 OSS 作为长期语义记忆。长期事实仍以确认后的客户、商机、跟进和任务表为准。
- TDD 已覆盖自然承接、分析分流、上下文传入、助手回复记忆、租户/用户/聊天/过期隔离、缺失
  字段卡片、确认前零副作用和“关键词不能绕过非跟进意图”；Agent 全量 `142/142`，Postgres
  集成 `9/9`。服务端与测试类型检查、Lint、Agent/Web 构建均已通过，真实飞书 UI 的干净双轮
  复测仍待执行。

本节是当前实现口径。后文更早日期中出现的“确定性记录指令门”或“普通聊天不持久化记忆”是
历史 Red 证据，不应覆盖 D-025 的现行设计。

## 2026-09-22 运行态、无回复根因与回调修复

- 本轮复现时 Agent 进程仍监听 `0.0.0.0:3100`，但飞书后台仍指向已退出的旧 Tunnelmole
  地址 `xg8hdf-ip-183-23-52-218.tunnelmole.net`；旧公网地址返回 404，Tunnelmole 日志没有
  新消息请求，飞书事件日志也为空。因此“普通消息无回复”的直接根因是事件没有送达，暂非
  意图路由或模型异常。
- 已重新建立当前临时入口
  `https://hwchvr-ip-183-23-52-218.tunnelmole.net`，Agent 进程 PID `7752` 监听 3100。
  首页返回 HTTP 200；事件和卡片 `url_verification` challenge 均返回合法 JSON，并在飞书
  开发者后台回读确认已持久化：
  - 事件：`/webhooks/feishu/events`
  - 卡片：`/webhooks/feishu/cards`
- `FeishuWebhookBridge` 增加脱敏入口日志：HTTP 到达、消息映射、工作流开始/完成/失败及
  事件异常；只记录消息和租户哈希、类型和路径，不记录原始客户文本。
- 代码验证：服务端 TypeScript 检查通过；全量 Agent 测试 20 个文件、`137/137` 通过；
  Agent 构建通过。已重启使用最新编译产物的服务。
- 旧消息不会因回调地址修复而自动重投，当前待用户在“销售agent”私聊发送一条新的普通
  测试消息，以完成真实事件投递、等待态和最终回复 UI 验证；Agent 不代替销售点击业务确认。

## 2026-09-21 唯一成功卡与自然澄清修订

- 真实飞书截图显示，一次确认后出现两张内容相同的“跟进登记成功”。控制库只有一次
  `followup.version.confirmed.v1`、一次 `action.succeeded` 和一次 `card.result_sent`，
  Base 也只有一条跟进；排除重复执行和重复写入。
- 根因是旧呈现同时把原草案卡 patch 成完整成功卡，又新增一张完整成功卡。D-022 覆盖旧口径：
  正常路径只把原卡就地更新为唯一成功卡；仅当原卡更新失败时补发一张结果卡。条件项目风险卡
  仍在成功结果之后独立发送。
- 裸客户事实的澄清改为“收到。你希望我把它整理成一份待确认的跟进记录，还是只分析它对
  商机的影响？”。D-023 要求用户侧文案只描述可选动作，不暴露“不会直接写入”等内部控制策略。
- TDD 先复现重复呈现与旧话术，随后定向回归 `47/47` 通过；全量回归 20 个测试文件
  `137/137`、Postgres 集成 `8/8`，三套 TypeScript、ESLint、Agent/Web 构建均通过。
  新版真实 UI 复测待本轮完成。
- 该次记录中的 `0i08bu...` 隧道已退出，现行入口以本文件顶部的 2026-09-22 运行态记录为准。

## 2026-09-21 确认保存与成功回执修复

- 真实使用发现：草案卡编辑后点击“确认保存”，只发生 `followup.quality.reviewed.v1`，动作仍停在
  `pendingConfirmation`，所以没有 Base/Task 写入和成功结果卡。
- 按 D-021 修复：“检查修改”只更新草案和质检；“确认保存”用当前表单值完成最后检查后当次获取执行权，不因字段改动要求第二次确认。
- `AgentExecutionResult` 现在保留客户、商机、跟进三个 Base `record_url`；原草案卡就地显示
  “跟进登记成功”和“查看跟进记录”，有任务时额外显示任务链接；成功 patch 追加
  `card.source_finalized`，只有 patch 失败后的补发才追加 `card.result_sent`。
- 本轮 Red/Green 与当时全量回归均通过：20 个测试文件、`136/136`，Postgres 集成 `8/8`，服务端/客户端/测试类型检查、ESLint、Agent/Web 构建均通过。后续 D-022/D-023 回归将当前基线提升为 `137/137`。
- 已重启 Agent PID `53240`，stderr 为空；当前临时公网入口为 `https://qyavyq-ip-183-23-52-218.tunnelmole.net`。本轮未替用户点击任意真实确认按钮；新版成功卡和 Base 记录链接仍需用一次新消息完成飞书 UI 复测。

## 2026-09-21 B0 多轮澄清复测：过期契约已验证，干净状态复测待继续

- 真实截图已复现“完整客户沟通 → Agent 问记录还是分析 → 销售说写跟进呀 → Agent 重复
  同一问题”。根因是第一轮未保存等待选择的短期会话，第二轮仅处理当前一句。
- 已批准的修订要求保存受租户、用户、聊天和 TTL 约束的澄清状态；确认记录只复用上一条
  原文，只分析保持零业务副作用，跨边界/超时/换话题不得承接。
- 截图中的 `2026-09-19 18:00` 已过期。跟进仍可保存，但候选待办必须禁用并提示修改时间。
- 首次真实双轮复测已发送：第一轮正确澄清，第二轮“写跟进呀”未重复澄清，但在保存前检查
  卡发出前失败。日志显示任务候选已正确标为 `pastDueAt`，持久化回读 Zod Schema 却漏了
  该合法枚举值；因此多轮原文承接已生效，失败点是 pending action 契约不一致。
- 已先增加序列化/回读失败测试并复现相同 Zod 路径，再补齐 Schema。最终全量为 20 个测试
  文件 134/134，Postgres 集成 8/8；三套 TypeScript、ESLint、Agent/Web 构建全部通过。
- 修复版已在 PID 58168 运行；本地和当前临时公网入口均为 200，启动 stderr 为空。本次未
  点击保存，也没有进入 Base/Task 执行链。当前状态仍是 `UI Red / fix deployed / retest
  pending`；再次发送真实复测消息前需获得用户新授权。
- 新授权后的第一条修复复测消息已成功生成草案卡，证明 `pastDueAt` 修复生效：过期待办
  显示“执行时间已过期，请修改”、不可勾选，保存按钮明确“不创建待办”。但上次失败遗留的
  intent clarification 会话仍处于打开状态，新原文被当作旧会话补充，未形成干净的“原文 →
  澄清 → 写跟进呀”链。
- 当前草案只处于 `pendingConfirmation`，无确认/执行审计，任务映射为 0；第二条“写跟进呀”
  已暂停，避免生成空白表单或重复草案。需用户确认取消当前未保存草案并重新发送两条消息。

## 2026-09-21 14:35 问答无回复与等待态

- 用户在真实“销售agent”私聊发送“你可以帮助我做什么”后没有机器人消息。开发者后台
  的事件请求地址仍为旧临时隧道 `qyavyq…`；本地 3100 返回 200，但旧公网入口返回 404，
  且没有隧道进程。直接原因是事件未送达，而非意图分类错误；旧消息不会自动重放。
- 按 SDD/TDD 增加普通问答文本等待态：先发“正在组织回答…”，模型返回后编辑同一条
  文本为答案。模型失败改为明确错误；编辑失败补发终态并审计；初始状态发送失败仍尝试
  最终答案。不给用户展示模型隐藏推理，不影响明确跟进的卡片确认链。
- Red 为定向 29/34，Green 为 34/34；最终全量 20 个测试文件 126/126，三套 TypeScript、
  ESLint、Agent/Web 构建通过。
- 新 Agent PID 54704、新临时隧道 PID 19968；公网入口
  `https://qwx2jz-ip-183-23-52-218.tunnelmole.net` 返回 200。飞书事件/卡片回调均已更新，
  两次 challenge 均为 201。真实飞书等待态与最终答案尚待一条新问答消息 UI 复验；
  临时隧道仍不具备正式运行保障。

## 2026-09-21 B0 真实 UI 复测与缺陷闭环

- 已在真实“销售agent”私聊验证问候、销售方法问答、内容生成、未接通业务查询、裸事实歧义
  澄清和明确记录跟进。前五类未进入跟进链；明确跟进生成新版“保存前检查”卡，不显示
  0–100 分或 A–D 等级。
- 首轮 UI 发现取消后原卡恢复按钮，以及“今天上午通过飞书”被误报为缺少沟通方式/时间并
  泄漏内部字段名。规格、决策问答和追踪矩阵已同步更新，旧现象保留为 Red 证据。
- 已扩展结构化提取契约以区分本次沟通方式/时间/主题与下一步方式；表单显式值优先，文本
  提取值会映射到保存前检查。相对时间同时传入 UTC instant、业务时区本地时间与 timezone，
  `Asia/Shanghai` 输出显式 `+08:00`。
- 取消会先持久化状态，并在卡片回调完成后延迟按 `cardMessageId` patch 原消息为不可操作
  终态；重复取消是幂等自愈入口。patch 失败不回滚 `cancelled`、不写 Base/Task，并追加失败
  审计。卡片建议统一中文化、去重，未知内部字段丢弃。
- 第一轮 TDD Red 为 41/45、Green 为 46/46；真实 UI 随后暴露同步 patch 竞态。第二轮新增
  28 项定向测试，Red 为 25/28，Green 为 28/28。最终全量 20 个文件 121/121，三套
  TypeScript、ESLint、Agent/Web 构建全部通过。
- 真实飞书复测 `B0 修复复测-20260921-1015` 已正确显示本次沟通方式“飞书”、时间
  `2026-09-21T10:15:00+08:00`、主题“试点方案”，并区分下一步“飞书会议”；卡片没有
  内部字段名、数字评分或字母等级。
- 同一张旧卡经重复取消自愈后，持久显示不可操作的“已取消销售动作”，再次读取未恢复。
  动作 `a4144be9-be97-46ef-b98a-07898f6df4b0` 为 `cancelled`，审计包含
  `card.cancel_patch_succeeded`，`effectAudits` 为空且不含任何确认或执行审计，证明未写
  Base、未创建任务。
- 最终 Agent 进程为 2652，监听 3100；本地和公网入口均为 200，stderr 为空。当前公网入口：
  `https://qyavyq-ip-183-23-52-218.tunnelmole.net`。

## 2026-09-20 对话入口与保存前反馈修订

- 产品确认机器人必须先理解意图，不能将所有私聊文本默认送入跟进提取。规格已增加 B0：
  普通聊天、销售方法问答、内容草稿、业务查询、明确跟进、操作、项目诊断和歧义澄清。
- B0 对话中枢已实现。AI 意图路由结合短期上下文；安全澄清门保证模型即使把裸事实误判为跟进，
  也只能先澄清，不能生成待确认动作、写 Base 或创建任务；当前跟进补充态同时绑定租户、用户
  和原聊天。
- 旧确认卡把跟进事实、记录完整度、项目风险和销售表现混成 0–100/A–D，已被新决策覆盖。
  默认卡已改为事实摘要、是否可保存、必须确认项、建议补充和待办预览；内部旧快照可兼容。
- 本轮定向测试 40/40、全量 20 个测试文件共 117 项全部通过；服务端/测试类型检查、ESLint、
  Agent 与 Web 构建通过。真实模型只读探测覆盖问候、方法问答、内容生成、业务查询与模型
  误判安全门。最新 Agent 已在 3100 端口重启，stderr 为空，本地与公网入口均返回 200。
- 本节不宣称已经完成新版飞书 UI 验收。旧 73/B、78/B 的真实 UI 证据继续证明历史版本能
  运行，但不能证明本轮无评分主视觉的新卡片和自然问答已经完成真实 UI 验收。
- 决策问答、产品/业务/技术架构、PRD、信息架构、系统契约、Phase B 和追踪矩阵已同步更新；
  实现和自动化证据见 `phase-b0-conversation-foundation.md`，真实 UI 证据待追加。

## 2026-09-20 原聊天 Card 2.0 文本闭环真实 UI 验收

> 2026-09-21 修订：真实使用发现，编辑字段后点“确认保存”被旧分支
> 误当成“检查修改”，动作仍停留在 `pendingConfirmation`，因此没有 Base/Task
> 执行，也不可能发送结果卡。当前修订目标是让确认按钮一次完成，并在
> 结果中保存 Base `followupRecordUrl`，成功卡提供“查看跟进记录”和可选的
> “打开飞书任务”。本轮自动化和真实 UI 复测证据尚未追加，不用旧验收替代。

- 已确认机器人文本、语音、妙记/会议纪要或飞书文档来源，都在提交内容的原聊天完成
  草案编辑、保存前内容质检、任务预览、一次确认和结果反馈，不强制跳 Web 确认。
- 目标为三种条件卡：草案＋实时内容质检＋待办预览；执行完成后把同一张草案卡更新为
  成功结果；出现新风险、关键变化或主动分析时另发项目推进质检。普通流程始终只占一个
  卡片消息，不固定发送多张。
- 本人任务与跟进在草案卡一次确认，执行时先写 Base，再自动创建卡中已预览、已选择的任务。
  跨角色通知、AI 任务状态建议、提醒和报告各自受证据、权限、策略、确认与去重约束。
- 决策问答、产品需求、交互、角色、Phase B、交付计划及追踪矩阵已同步修订；此前 P0 和
  Web UI 的历史证据仍保留，本节补充新版聊天卡的独立真实证据。
- 已实现机器人“写跟进/录入跟进”命令打开原聊天 Card 2.0 录入表单；包含客户、联系人、
  沟通方式/时间/主题/原文、下一步时间、方式/地点和参与人。提交后立即返回“生成中”，
  后台完成模型生成与保存前质检后更新原卡；失败恢复预填表单，期间不写 Base/Task。
- 已实现原聊天 Card 2.0 草案表单：跟进正文、保存前评分/建议、客户/联系人、下一步时间、
  方式/地点、参与人及待办选择；支持“保存修改并重新质检”和“确认保存并创建所示待办”。
- 卡片点击“检查修改”时追加新版本并重算检查/候选，不写 Base；
  点击“确认保存”时，以当前表单值作最后检查并当次执行，不再要求二次确认。
  空任务选择只写 Base，选中候选才在三项 Base 写入后建任务。
- 现行实现把原草案卡就地更新为唯一结果卡，并区分“待办已创建”和“本次未创建待办”。
  保存成功后，只有
  本次已确认原文命中明确负向/停止推进、竞品介入、售前/产品支持请求或明确逾期行动时，
  才按“原卡成功终态 → 条件项目风险卡”顺序发送额外风险卡；预算或决策链未知不会单独触发。
  风险卡显示逐条原文证据、建议，并明确“样本不足，暂不生成项目健康度评分”。跨角色推送、
  语音、妙记/会议、文档、任务状态回收、提醒和日报仍未实现。
- 真实飞书聊天已完成文本跟进录入、字段补充、AI 草案生成、v2/v3 保存前质检和一次确认。
  最终评分 73/B，无强制修改项；预算和决策链明确保留为未知，没有触发第三张风险卡，符合
  “未知不单独推风险卡”的条件规则。
- 本轮动作 `1fffcf75-f606-4c65-b3e8-d171bd9f6cd9` 仅有一次
  `followup.version.confirmed.v1` 和一次 `action.succeeded`。客户 `recvvKXMCHeUK2`、商机
  `recvvKXNoHC16k`、跟进 `recvvKXO88qZRX` 已在 Base UI 对账；任务
  `66bbc267-e50c-476e-a01c-9ffd100fbe5c`（`t142596`）已在任务 UI 核对为未完成，负责人
  李胜彬，截止时间 2026-09-22 14:00，标题为“在客户现场与张总、售前王工进行技术交流”。
- 历史验收时原卡和独立结果卡都显示完成；该双卡呈现已被 D-022 覆盖。现行实现只保留
  原卡就地更新后的唯一成功卡。
  修复采用持久化 `message_id` 的 `im.message.patch`，不依赖可能被回调响应覆盖的临时卡更新。
  日期控件同时改为先按 `Asia/Shanghai` 转换，再传给 `picker_datetime`，避免 UTC 日期错位。
- 当前长期 Goal 仍在推进中。20 个测试文件、117 项测试、前后端/测试类型检查、ESLint、
  Agent 与 Web 构建均通过。历史文本双卡链路已 `UI Verified`，但现行单成功卡仍待复测；
  带明确风险证据的条件风险卡、
  无任务、部分失败等真实 UI 分支仍待后续验收。
- 当前服务公开地址为 `https://yaagob-ip-113-87-154-44.tunnelmole.net`；飞书事件 URL 与卡片
  回调 URL 已在开发者后台保存并通过页面核对。临时隧道重启后仍会失效。

## 2026-09-20 Phase B 真实 Web 闭环验收

- Web `/followups/new` 已有表单/文本录入；`/followups/:id/edit` 已有草案正文、评分、
  风险/建议、客户/下一步/截止时间/证据编辑、再质检及确认写入操作。确认服务经可信
  session 检查本人和租户，冻结版本并复用 P0 Base/Task 幂等执行链。
- 真实 Web UI 已完成表单输入、智谱生成、人工编辑、v3 再质检和确认。评分从 73 提升到
  77；修复后 `communicationMethod` 与 `communicationAt` 不再误报缺失。确认后页面冻结为
  v4，并显示执行状态 `succeeded`。
- 本轮真实外部结果：客户 `recvvIZt8oaVR3`、商机 `recvvIZtXDfkrg`、跟进
  `recvvIZuSkdWei`、任务 `1bc3e76a-90af-48b5-ad36-93390bc3b804`。飞书 Base UI 已看到
  三表记录；Task v2 回读确认标题、本人负责人、`todo` 状态与 2026-09-22 18:00 截止时间。
- 该轮 Web UI 使用的旧临时域名已经失效；当前临时域名见上一节。临时隧道重启后仍会失效。
- 该轮自动化基线为 11 个测试文件、72 项测试；当前基线已提升为 20 个测试文件、117 项测试。
  前后端/测试类型检查、ESLint、Agent 与 Web 构建均通过。两租户幂等与跨租户拒绝的 30 项
  定向回归通过。该轮动作在控制库只有一条
  `succeeded` 执行结果、一次确认审计和一次成功审计，没有重复外部副作用。
- 机器人 Card 2.0 文本录入/草案/结果卡已真实 UI 验收；条件项目风险卡已实现和自动化，
  但带风险证据的第三卡仍待真实 UI 验收。语音、妙记/会议、文档、主管推送、任务状态同步
  和个人日报尚未实现。

以下 Phase A 与 P0 章节保留历史证据；其中“当前隧道/当前基线”的旧说法以本节为准。

## 2026-09-19 Phase A 增量

- 独立 Web 入口已可通过飞书 OAuth 登录，当前账号真实进入工作台，OAuth 返回姓名会同步到
  租户成员；没有为此新增通讯录读取权限。
- 多租户成员、角色、汇报关系、资源授权、Web session 与追加式权限审计已落 Postgres。
- 已完成桌面/窄屏 UI、管理员允许和销售直达管理页拒绝验证；当前公开地址是临时隧道，重启后
  可能变化，不属于生产固定域名。
- 自动化基线为 51 项单测、5 项真实数据库集成测试，前后端类型检查和构建通过。
- Phase A 尚未完成四真实角色账号与第二真实企业验收；Phase B 的 Web 表单/生成/质检以及
  新版机器人文本卡片闭环已 UI 验收，多输入适配器仍未实现。

## 1. 审计结论

当前目录包含遗留妙搭脚手架，以及一套不加载妙搭运行时的独立销售 Agent 服务。

```text
当前已有：Agent 后端 + 飞书事件/卡片入口 + LLM + Base/Task 适配器 + 控制库 + 测试
当前状态：1.0.1 已发布 + Base 应用访问已通过 + 真实 P0 闭环已验收
```

## 2. 飞书侧资产

| 资产 | 标识 | 用途 | 当前状态 |
| --- | --- | --- | --- |
| 飞书自建应用 | `销售agent` / `cli_aa211d0457381bdf` | P0 机器人与卡片测试入口 | `1.0.1` 已审核发布 |
| 妙搭应用 | `销策 Agent` / `app_17e9pwd9246` | 上一版托管底座 | 遗留，不进入目标架构 |
| Demo 多维表格 | [销售 Agent P0 Demo](https://ldkj.feishu.cn/base/Kms1bAC9pahewCsxUNEc9oTPnhd) | 模拟客户、商机、跟进 | 已创建并回读验证 |
| 飞书任务联动 | Task v2 | 承接下一步行动 | 真实创建并回读通过 |

`cli_` 开头的是飞书开放平台应用 ID，`app_` 开头的是妙搭应用 ID，两者不是同一种资产。

自建应用的旧 Secret 曾在对话中暴露。用户报告已重置，新值只配置在 Git 忽略的本地文件；
目标应用身份换取 Token 已成功，仓库内未记录密钥。

2026-09-18 用户切换账号后已能访问目标应用；控制台显示其所属企业为
“轮动|AI数字化解决方案专家”，与 Demo Base 的轮动云文档环境一致。只读检查确认原版本
`1.0.0` 已发布，但此前没有机器人能力、API 权限或事件订阅。机器人能力现已添加；所需
权限已开通，`task:task:write` 可指定人员限定为当前测试用户。事件与
卡片回调已更新到本轮重新建立的临时 HTTPS 隧道，并分别添加
`im.message.receive_v1` 和 `card.action.trigger`。两条公网 URL challenge 均已通过。
版本 `1.0.1` 已包含机器人、事件、回调和首批九项权限，并已由企业管理员审核发布。发布后
补充了真实 API 返回要求的免审权限 `base:record:retrieve`，应用身份已可查询 Base 记录。

### Demo Base 资源

| 表 | table_id | 字段数 | 验证状态 |
| --- | --- | ---: | --- |
| 客户 | `tbl04z7kYNpPQpBe` | 5 | 已回读 |
| 商机 | `tblyeyIIQpBOaaz8` | 7 | 已回读，关联客户指向客户表 |
| 跟进 | `tbliVwB7fEDdgMjN` | 12 | 已回读，关联客户/商机均指向真实表 |

2026-09-18 通过飞书 Base 接口回读全部 24 个字段的类型；文本、金额、日期、人员和关联
字段均与本机租户映射一致。首次结构检查时未写记录，随后真实闭环写入了三条 P0 记录。

发布后已获得目标机器人身份，并将机器人作为编辑协作者加入 Demo Base。目标应用身份已
读取全部 3 张表、24 个字段，并回读本次真实 P0 记录。资源错误 `91403` 已解除。

真实验收记录：客户 `recvvyXwhCspS3`、商机 `recvvyZqewDQwe`、跟进
`recvvyZr25Fl4S`。商机和跟进的关联字段均回指对应真实记录；客户名、联系人、金额、进展、
下一步和截止时间均与用户确认卡片一致。所有记录都使用 `P0测试` 前缀。

当前登录用户所在企业的 `tenant_key` 已通过飞书官方用户信息接口确认，并写入 Git 忽略的
`migrations/004_agent_p0_demo_tenant.local.sql`。目标应用和 Base 的企业归属已确认一致。

## 3. 现有文件来源

### 妙搭官方脚手架

- `client/`：React 19、Vite、Tailwind 和飞书业务 UI 组件，当前首页仍是欢迎页。
- `server/`：NestJS 服务端骨架；独立 `agent.main.ts` / `agent-app.module.ts` 注册销售
  Agent，不经过原妙搭 `app.module.ts`。
- `scripts/`：妙搭提供的开发、构建和检查脚本。
- `.spark/meta.json`：妙搭应用绑定信息。
- `.agents/skills/`：妙搭工程内置的编码、权限、插件和触发器规范。
- `node_modules/`：第三方依赖，不是业务代码。

这些文件数量较多，主要来自组件库和依赖安装，不代表已完成大量业务功能。

### 上一版方案新增内容

- `migrations/001_sales_agent.sql`：面向妙搭 Postgres 的销售表结构。
- `migrations/002_demo_seed.sql`：旧开发库演示数据。
- `server/capabilities/*.json`：妙搭 AI 插件能力草案。
- `server/database/schema.ts`：从妙搭开发库生成的 Drizzle 类型。
- `package.json`：声明了三个妙搭 AI 插件包。

以上数据库与插件配置不属于新目标架构。P0 为避免破坏遗留资产暂时保留；后续可单独决定
删除或隔离到 `legacy/`。

## 4. 本轮文档产物

| 文件 | 状态 |
| --- | --- |
| `README.md` | 已按新目标改写 |
| `docs/decision-log.md` | 已新增，记录关键决策 |
| `docs/agent-experience.md` | 已按机器人 + 卡片 P0 改写 |
| `docs/product-architecture.md` | 已按多企业 SaaS 改写 |
| `docs/business-architecture.md` | 已按 Base 数据源和确认流程改写 |
| `docs/technical-architecture.md` | 已按独立 Agent 后端改写 |
| `docs/feishu-app-setup.md` | 已新增，记录飞书应用能力与权限前置条件 |
| `docs/p0-acceptance.md` | 已新增，记录 Goal 完成证据与状态 |
| `docs/reference-capability-map.md` | 已重新划分 P0 与后续范围 |
| `docs/decisions/2026-09-20-followup-chat-cards.md` | 已新增，保留卡片、质检、任务与推送的决策问答及被覆盖口径 |

## 5. 新架构实现

- `server/agent.main.ts` 与 `server/agent-app.module.ts`：独立 NestJS 入口，不加载
  `PlatformModule`。
- `server/modules/agent-core/`：会话、确认状态机、卡片和重试编排。
- `server/modules/control-store/`：独立 Postgres 控制数据和原子状态迁移。
- `server/modules/llm/`：OpenAI-compatible 结构化提取与证据校验。
- `server/modules/feishu/`：SDK 客户端、消息卡片、事件和卡片 Webhook。
- `server/modules/integrations/base/`：客户、商机、跟进查询与写入。
- `server/modules/integrations/task/`：Task v2 幂等任务创建。
- `tests/unit/agent-workflow.spec.ts`：关键状态机与两租户隔离测试。
- `migrations/003_agent_control.sql`：多租户控制数据表。
- `migrations/004_agent_p0_demo_tenant.example.sql`：Demo 租户与 Base 字段映射模板，不含密钥。
- `.env.example`：本地运行所需变量名模板；真实 `.env.local` 已由 Git 忽略。
- `vitest.config.ts` 与 `tsconfig.test.json`：Agent 自动化测试配置。

临时 HTTPS 隧道已完成两类公网 URL challenge 和真实事件/卡片回调。真实业务 Agent 正在
3100 端口运行。本地 Postgres、智谱免费模型地址/名称、有效 API Key 和重新轮换后的
Verification Token 已配置。P0 端到端链路已经完成。

## 6. 当前阻塞信息

当前没有阻止 P0 Demo 运行的配置缺口。仍需注意以下运行风险：

1. 临时 HTTPS、消息事件和卡片回调已更新并通过公网 challenge；隧道只用于 P0 联调，
   进程终止后地址会失效。
2. 智谱免费模型可能因访问量过大返回 `429` / 模型码 `1305`；代码会有限退避重试，
   最终失败时不写 Base、不创建任务，并向用户明确反馈。
3. P0 使用自建应用和单个真实企业验证；跨企业分发仍需 ISV/商店应用或每企业独立应用。

本地 P0 控制库已使用 PostgreSQL 17 初始化，监听 `127.0.0.1:55432`，完成控制表迁移并
写入一条 Demo 租户配置。数据与便携运行时保存在 `%LOCALAPPDATA%/SalesAgentP0`，不在
Git 工作区内、不监听公网。`.env.local` 已写入本地连接串。智谱配置使用官方普通 API
`https://open.bigmodel.cn/api/paas/v4` 和免费模型 `glm-4-flash-250414`。API Key 已填写，
真实调用最终返回 HTTP 200，JSON 输出符合预期。首次请求曾收到模型拥堵 `429`，有限
重试后成功。飞书 Verification Token 已在后台轮换并安全写入
`.env.local`；事件和卡片两个本地 URL challenge 均返回 201 且 challenge 匹配。

## 7. 完成度口径

真实飞书消息已触发 Agent，确认后成功写入 Base 并创建飞书任务；两个模拟租户隔离测试
同时通过。按本文件的完成度口径，P0 Demo 已完成。

## 8. LangChain/LangGraph 与长期记忆当前进展

- LangGraph Postgres Checkpointer 是当前唯一工作流状态层，已覆盖 thread 恢复、短期上下文、
  确认状态和租户/销售/聊天边界。
- `mem0ai@3.2.0` 已安装，并新增 `LongTermMemoryPort` 与 `Mem0MemoryAdapter`；Graph 分类前
  已按 tenant + actor 召回 ACL 过滤偏好，`memory_save` 已通过批准门写入；`MEM0_ENABLED=false`
  时不初始化外部向量后端并返回可见降级。
- 适配器只接受显式批准的销售/用户偏好，写入带 `tenant_id`、`actor_open_id`、来源引用和
  策略版本；查询、删除和 history 在应用层再次做 ACL 校验。
- 已新增 [Mem0 长期记忆 SDD](specs/11-mem0-long-term-memory-sdd.md) 和
  [Mem0 长期记忆 TDD](specs/12-mem0-long-term-memory-tdd.md)。单元测试覆盖默认关闭、边界、
  ACL、召回注入和批准写入；真实 Qdrant、中文召回、容量和恢复演练仍是后续上线门禁。

### 当前 TODO

1. 选定并部署持久向量后端，完成 embedding、中文召回和容量评测；
2. 对真实后端做过期、备份恢复和两租户隔离演练；
3. 评测通过后再在目标环境开启 `MEM0_ENABLED`。
